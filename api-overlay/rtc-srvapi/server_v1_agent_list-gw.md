---
examples:
  type: regsip
descriptions:
  type: 按接入方式筛选，只返回支持该方式的网关
---

列出可用的**设备网关**。登记设备前先调它拿 `gw` 的取值。

设备网关是介于外部设备与 RTC 之间的转换层：SIP/H323/国标各有自己的信令协议，由网关负责对接，并把媒体转成 RTC 能用的格式。不同网关支持的接入方式不同，所以用 `type`（URL 查询参数）筛选。
